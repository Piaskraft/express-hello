<h2>Kontakt (POST)</h2>

<form action="/contact/send-message" method="POST">
  <label>Imię i nazwisko:</label>
  <input type="text"   name="author"  value="{{author}}">

  <label>E-mail:</label>
  <input type="email"  name="sender"  value="{{sender}}">

  <label>Tytuł:</label>
  <input type="text"   name="title"   value="{{title}}">

  <label>Wiadomość:</label>
  <textarea name="message">{{message}}</textarea>

  <button type="submit">Wyślij</button>
</form>

{{#if isSent}}  <p class="success">✅ Wiadomość od {{author}} została wysłana.</p>{{/if}}
{{#if isError}} <p class="error">❌ Uzupełnij wszystkie pola.</p>{{/if}}
